#include "../include/common.hpp"
#include <fstream>
#include <iostream>

TransactionDB TransactionDB::LoadDatabase(const std::string& filepath) {
    TransactionDB db;
    // TODO: Implement parsing logic to read integers from lines into db.transactions
    // and dynamically determine db.max_item_id.
    return db;
}
